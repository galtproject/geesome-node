# Database Scalability Inventory

## Source Of Truth

Original request for this slice: add a review of database scalability for storing hundreds of thousands of posts and content records in groups.

This file is generated by `npm run database:scalability:update`. Do not hand-edit the tables; update `check/databaseScalabilityInventory.ts` or the source code and regenerate.

## Summary

- Model/query areas reviewed: 15
- Query hotspots reviewed: 19
- Primary risk area: group post timelines and generated manifests at 100k+ posts per group.
- Current review doc: [database-scalability-review.md](database-scalability-review.md).

## Model Index Inventory

| Area | Model | Current Index Signals | Review Notes | Source |
| --- | --- | --- | --- | --- |
| Group posts | Post | name; name,isRemote unique local; replyToId; repostOfId; source,sourceDate; source,sourceChannelId; source,sourceChannelId,sourcePostId | has group index; has group/local lookup index; has manifest lookup index | `app/modules/group/models/post.ts` |
| Post contents join | PostsContents | postId,position order index; contentId reverse index | join lookup indexes present; API timeline hydration is page-scoped; remaining work is attachment constraints and body projection | `app/modules/group/models/post.ts` |
| Groups | Group | name,isRemote unique local non-collateral; manifestStorageId; manifestStaticStorageId; isDeleted,staticStorageUpdatedAt static rebind index | missing creator/type/deleted listing index; membership/admin through indexes present | `app/modules/group/models/group.ts` |
| Contents | Content | storageId,userId non-unique ownership lookup; manifestStaticStorageId unique; userId,manifestStorageId actor-scoped lookup | has user listing index; has manifest lookup index; has preview lookup indexes; same storageId across different users is valid; remaining global storage/manifest findOne paths need caller-specific actor scope or canonical asset semantics | `app/modules/database/models/content.ts` |
| Static ID history | StaticIdHistory | staticId,dynamicId unique; staticId,boundAt; dynamicId lookup index | group/static manifest churn can create long history; dynamicId lookup is indexed, latest-binding/retention still open | `app/modules/staticId/models.ts` |
| Content accounting | UserContentAction / UserLimit | content action user index; user limit user,name index | quota checks sum UserContentAction by userId/name/createdAt before uploads; ledger size is BIGINT; UserLimit lookup index is present but uniqueness is still separate | `app/modules/database/models/userContentAction.ts` |
| Async operations | UserAsyncOperation / UserOperationQueue | userId,inProcess,name,createdAt operation-list index; inProcess,updatedAt restart-sweep index; queue module,isWaiting picker index | operation-list and restart-sweep indexes are present; retention/cleanup policy remains open | `app/modules/asyncOperation/models.ts` |
| Auto actions | AutoAction | isActive index; isActive,executeOn executor index | due active executor scan is indexed; executor locking/batching policy remains separate | `app/modules/autoActions/models.ts` |
| Pin accounts | PinAccount | legacy name,userId,groupId uniqueness; userId,name lookup index; groupId,name lookup index | hot owner/name lookups are indexed; uniqueness semantics still use the legacy name-leading composite | `app/modules/pin/models.ts` |
| Group permissions | GroupPermission | explicit permission index | permission lookup indexes present; uniqueness/share semantics still separate | `app/modules/group/models/groupPermission.ts` |
| Unread state | GroupRead | userId,groupId unique | supports one read cursor per user/group; count is indexed but cursor is timestamp-only | `app/modules/group/models/groupRead.ts` |
| Social imports | SocNetImport* | channel/message source indexes present | import duplicate checks are mostly indexed; reversal paths reuse group timeline queries | `app/modules/socNetImport/models.ts` |
| Static sites | StaticSite | entity/user lookup indexes present | static generation scalability is dominated by group post loading, not staticSite lookup | `app/modules/staticSiteGenerator/models.ts` |
| Categories and sections | GroupCategory / GroupSection pivots | category name; section name,categoryId unique; section parentSectionId; categoryGroups categoryId,groupId; categoryGroups groupId,categoryId; groupSections sectionId,groupId; groupSections groupId | category admin/member/group through indexes are present; section pivot indexes are present; feed cursor/count policy still needs review | `app/modules/groupCategory/models/groupCategory.ts` |
| Tags and mentions | Tag / TaggedPosts / Mention / AutoTag | no explicit tag indexes; taggedPosts through table defined; no explicit mention lookup indexes; no explicit autoTag lookup indexes | quiet today, but tag/mention/federation filters need post/tag and source/target indexes before large feeds | `app/modules/group/models/tag.ts` |

## Query Hotspots

| Area | Hotspot | Observed Pattern | Scalability Risk | Source |
| --- | --- | --- | --- | --- |
| Timeline API | getGroupPosts | keyset/cursor and legacy offset paths select page post IDs first, then hydrate contents/reposts for the bounded page | page selection avoids attachment joins; remaining risks are offset total counts and high-volume callers that request very large pages | `app/modules/group/index.ts` |
| Global post listings | getAllPosts | selects page post IDs first, then hydrates contents for the bounded page | content joins no longer drive page selection, but all-group offset scans still need cursor/export projections for high-volume use | `app/modules/group/index.ts` |
| Unread counters | getGroupUnreadPostsData | count posts newer than read cursor | count is backed by the timeline index, but read cursor remains timestamp-only | `app/modules/group/index.ts` |
| Group manifest generation | generateGroupManifest | loads the previous posts trie, scans changed/deleted/unpublished lightweight post refs in (updatedAt,id) cursor batches, then unsets removed local IDs | content/repost hydration and large changed-ref windows are avoided; rebuild still loads/copies the previous posts trie and rewrites a monolithic manifest | `app/modules/entityJsonManifest/index.ts` |
| Post visibility | getPostsWhere defaults | C1: default filter isDeleted=false AND status=Published; admin override via includeAllStatuses/status/statusIn | published default is in place; remaining risk is accidental admin override use in public/generated paths | `app/modules/group/index.ts` |
| Post writes | createPost / updatePost / deletePosts | canonical create/update/delete DB state is wrapped in transactions: localId allocation, post row, attachments, tombstone flag, size, reply/repost counts, and group counters | canonical post DB partial-state risk is reduced; import/upsert transitions and manifest/static derived work still need transaction/job boundaries | `app/modules/group/index.ts` |
| Static site rendering | prepareGroupPostsForRender | loads up to 9999 posts with contents before rendering pages | bounded but still heavy for media-rich groups; should batch IDs and contents | `app/modules/staticSiteGenerator/index.ts` |
| RSS rendering | groupRss | loads up to 9999 posts with contents for feed generation | feeds should cap lower or use a feed-specific projection | `app/modules/rss/index.ts` |
| Social import reversal | reverse-post import path | uses getGroupPosts with idGte filter | reversal scans now inherit page-scoped hydration, but should still use purpose-built groupId/id cursor scans when content is not needed | `app/modules/socNetImport/index.ts` |
| Category feeds | getCategoryPosts | joins group/category pivots only for page post ID selection, then hydrates contents/group for the bounded page | content joins no longer drive category page selection; pivot indexes are present, while offset/count cost and cursor migration remain open | `app/modules/groupCategory/index.ts` |
| Content preview serving | getContentByStorageId(findByPreviews) | OR lookup across storageId and preview storage ids | preview columns are indexed, but public/header serving still reads across user-owned rows instead of canonical asset metadata | `app/modules/database/index.ts` |
| Content deletion | deleteFileCatalogItem deleteContent | destroys catalog item first; only destroys content/physical storage after DB reference checks | DB row references are covered; generated output, durable pin state, and async garbage collection still need a fuller lifecycle | `app/modules/fileCatalog/index.ts` |
| Quota checks | checkFileSizeAndSaveByStream | quota sum before upload stream, action recorded after content creation | accounting SUM is indexed, but concurrent uploads can still pass before reservation/action recording | `app/modules/content/index.ts` |
| Async operation ledger | getUserAsyncOperationList / closeAllAsyncOperation | lists by user/inProcess/name ordered by createdAt and closes all in-process rows on startup | lookup and startup sweep indexes are present; finished-operation retention remains open | `app/modules/asyncOperation/index.ts` |
| Auto action executor | getAutoActionsToExecute | periodic executor selects active actions due before now | due/active range scan is indexed; batching and duplicate executor locking remain separate scheduler concerns | `app/modules/autoActions/index.ts` |
| Pin account lookup | getUserAccount / getGroupAccount | looks up pin accounts by owner id plus name | hot owner/name lookups are indexed; legacy uniqueness still starts with name | `app/modules/pin/index.ts` |
| List parameter normalization | prepareListParams | normalizes limit/offset to non-negative integers, caps limit, allowlists simple sort columns, and clamps sort direction | bad public list params are normalized before Sequelize; endpoint-specific sort allowlists and lower public max page sizes remain open | `app/helpers.ts` |
| Static rebind scan | getGroupWhereStaticOutdated | filters non-deleted groups whose staticStorageUpdatedAt is older than the threshold | scan predicate is indexed; cron remains disabled and should probably become event-driven before high churn | `app/modules/group/index.ts` |
| Static ID resolution | getStaticIdItemByDynamicId | dynamicId lookup ordered by boundAt | dynamicId lookup is indexed, but manifest churn still needs latest-binding and retention policy | `app/modules/staticId/index.ts` |

