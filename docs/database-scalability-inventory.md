# Database Scalability Inventory

## Source Of Truth

Original request for this slice: add a review of database scalability for storing hundreds of thousands of posts and content records in groups.

This file is generated by `npm run database:scalability:update`. Do not hand-edit the tables; update `check/databaseScalabilityInventory.ts` or the source code and regenerate.

## Summary

- Model/query areas reviewed: 21
- Query hotspots reviewed: 28
- Primary risk area: group post timelines and generated manifests at 100k+ posts per group.
- Current review doc: [database-scalability-review.md](database-scalability-review.md).

## Model Index Inventory

| Area | Model | Current Index Signals | Review Notes | Source |
| --- | --- | --- | --- | --- |
| Group posts | Post | name; name,isRemote unique local; replyToId; repostOfId; source,sourceDate; source,sourceChannelId; source,sourceChannelId,sourcePostId; groupId,source,sourceChannelId,sourcePostId unique source identity | has group index; has group/local unique identity; has group/source unique import identity; has manifest lookup index | `app/modules/group/models/post.ts` |
| Post contents join | PostsContents | contentId,postId through primary key; postId,position unique order index; contentId reverse index | join lookup indexes present; attachment positions are unique per post; attachment authorization reasons are explicit; API timeline hydration is page-scoped; text/json body projection can reuse a bounded per-render cache | `app/modules/group/models/post.ts` |
| Post events | PostEvent | postId,createdAt,id event stream; groupId,createdAt,id group event stream; groupId,source,sourceChannelId,sourcePostId,createdAt,id import event stream; type,action,createdAt,id event filter | model-sync-created append-only post event table is ready for source-identity import audit/replay rows | `app/modules/group/models/postEvent.ts` |
| Groups | Group | name,isRemote unique local non-collateral; manifestStorageId; manifestStaticStorageId; creatorId,type,isDeleted,createdAt,id creator list index; isDeleted,staticStorageUpdatedAt static rebind index | has creator listing index; membership/admin through indexes present | `app/modules/group/models/group.ts` |
| Contents | Content | userId,storageId unique ownership constraint plus storageId,userId lookup; manifestStaticStorageId unique; userId,manifestStorageId actor-scoped lookup | has user listing index; has manifest lookup index; has preview lookup indexes; same storageId across different users remains valid; same-user duplicates are guarded by cleanup-backed uniqueness; shared storage/manifest reads use deterministic id ordering, and new library rows require an actor until canonical assets exist | `app/modules/database/models/content.ts` |
| Object cache | Object | storageId,resolveProp unique cache key | active data-structure cache keys resolved and unresolved storage paths separately | `app/modules/database/models/object.ts` |
| File catalog | FileCatalogItem | contentId reverse index; user,parent,isDeleted,type,createdAt,id folder listing index; cleanup-backed active path uniqueness for child and root rows | content reference checks, large folder listings, exact active path writes, and concurrent default upload folder creation are covered | `app/modules/fileCatalog/models.ts` |
| Static ID history | StaticIdHistory / StaticIdBinding | staticId,dynamicId unique; staticId,boundAt; dynamicId lookup index; staticId unique current binding; dynamicId,boundAt current lookup | hot static and dynamic resolution use compact current-binding rows created by model sync; StaticIdHistory keeps bounded per-static audit history and preserves the current binding | `app/modules/staticId/models.ts` |
| Content accounting | UserContentAction / UserLimit | content action user index; user limit user,name unique | quota checks sum UserContentAction by userId/name/createdAt before uploads, then content/action commits re-check under a UserLimit row lock; ledger size is BIGINT and UserLimit is unique per user/name after duplicate-key retry | `app/modules/database/models/userContentAction.ts` |
| Async operations | UserAsyncOperation / UserOperationQueue | userId,inProcess,name,createdAt operation-list index; inProcess,updatedAt restart-sweep index; queue module,isWaiting picker index; queue asyncOperationId lifecycle index; queue isWaiting,asyncOperationId,updatedAt,id retention index | operation-list, restart-sweep, queue lifecycle, and queue retention indexes are present; finished-operation retention cleanup is bounded on startup | `app/modules/asyncOperation/models.ts` |
| Auto actions | AutoAction | isActive index; isActive,executeOn executor index; isActive,executeOn,executeClaimExpiresAt,id claim index; userId,createdAt,id management-list index | due active executor scan, execution claims, and user management-list default paging are indexed; cron claims due actions atomically before queueing | `app/modules/autoActions/models.ts` |
| Pin accounts | PinAccount | cleanup-backed userId,name unique lookup; cleanup-backed groupId,name unique lookup | runtime owner/name lookups are deterministic after duplicate-name cleanup; owner-scoped account lists are capped and ordered by allowlisted fields | `app/modules/pin/models.ts` |
| Foreign accounts | ForeignAccount / AuthMessage | userId account-list index; userId,provider lookup index; provider,address lookup index; authMessage foreignAccountId index | user account lists are capped for the API while manifest export keeps the full internal list | `app/modules/foreignAccounts/models.ts` |
| Social network accounts | SocNetAccount | userId,socNet,phoneNumber lookup index; userId,socNet,username lookup index | user account lists are capped for the API and can filter by social network | `app/modules/socNetAccount/models.ts` |
| Telegram content bots | ContentBots | userId bot-list index | user content-bot lists are capped for the API and backed by the userId index | `app/modules/tgContentBot/models.ts` |
| Group permissions | GroupPermission | explicit permission index | permission lookup indexes present; uniqueness/share semantics still separate | `app/modules/group/models/groupPermission.ts` |
| Unread state | GroupRead | userId,groupId unique | supports one read cursor per user/group; unread count can use readAt plus readPostId tie-breaker | `app/modules/group/models/groupRead.ts` |
| Social imports | SocNetImport* | channel/message source indexes present | message duplicate checks and post source identity are constraint-backed; reversal paths use lightweight post refs when present | `app/modules/socNetImport/models.ts` |
| Static sites | StaticSite | entity/user lookup indexes present | static generation scalability is dominated by group post loading, not staticSite lookup | `app/modules/staticSiteGenerator/models.ts` |
| Categories and sections | GroupCategory / GroupSection pivots | category name; section name,categoryId unique; section parentSectionId; categoryGroups categoryId,groupId; categoryGroups groupId,categoryId; groupSections sectionId,groupId; groupSections groupId | category and section aggregate size fields are BIGINT; category admin/member/group through indexes are present; section pivot indexes are present; feed cursor/count policy still needs review | `app/modules/groupCategory/models/groupCategory.ts` |
| Tags and mentions | Tag / TaggedPosts / Mention / AutoTag | tag name index; taggedPosts post/tag indexes; mention FK indexes; autoTag group index | tag/mention/auto-tag lookup indexes are present; feed cursor/count policy still needs review before large tag surfaces | `app/modules/group/models/tag.ts` |

## Query Hotspots

| Area | Hotspot | Observed Pattern | Scalability Risk | Source |
| --- | --- | --- | --- | --- |
| Large fixture benchmark | database:scalability:fixture / explain / generated-output | seeds a 100k-post group fixture plus category, mixed image/text/json content metadata, attachment, quota, preview, and static-ID side rows, then emits current EXPLAIN ANALYZE probes and generated-output pressure signals against fixture/restored groups selected by name or id; optional category/content/static-ID probes are skipped when restored data lacks sample rows | future index/query changes and generated-output body/copy pressure can be measured against production-scale fixture or a specific restored group id; generated reports remain intentionally uncommitted | `check/databaseScalabilityFixture.ts` |
| Generated output pressure | database:scalability:generated-output | reports selected-post attachment rows, unique text/json body read candidates, RSS selected text reads, non-text storage copy candidates, and group-manifest inline/index pressure from a fixture/restored group selected by name or id | the large-output follow-up can distinguish persisted-snippet pressure from cache tuning, storage-copy pressure, and large-manifest cutoff pressure before changing runtime defaults or adding schema | `check/databaseGeneratedOutputPressure.ts` |
| Timeline API | getGroupPosts | keyset/cursor and legacy offset paths select page post IDs first, hydrate contents/reposts for the bounded page, and allow offset callers to skip totals | page selection avoids attachment joins; cursor pages and includeTotal=false skip counts, while default legacy offset callers can still request totals | `app/modules/group/index.ts` |
| Global post listings | getAllPosts | selects cursor-aware page post refs first, then hydrates contents for the bounded page; exports can iterate lightweight refs in cursor batches | content joins no longer drive page selection; high-volume all-post scans can avoid offsets through cursor ref batches | `app/modules/group/index.ts` |
| Unread counters | getGroupUnreadPostsData | count posts newer than the stored (readAt, readPostId) cursor when present, with readAt-only fallback | count is backed by the timeline index and can disambiguate same-timestamp posts; old readAt-only rows still use the legacy timestamp predicate | `app/modules/group/index.ts` |
| Group manifest generation | generateGroupManifest | loads the previous posts trie for compatibility manifests, resumes changed/deleted/unpublished lightweight post refs from a durable group (updatedAt,id) cursor with same-timestamp overlap, unsets removed local IDs, dual-writes a paged post-index sidecar, and can publish chunked-only manifests through an explicit option or count limit | content/repost hydration, timestamp-only watermarks, and large changed-ref windows are avoided; large groups can avoid the legacy inline trie when enabled, while default chunked-only rollout and page-level incremental updates still need follow-up | `app/modules/entityJsonManifest/index.ts` |
| Post visibility | getPostsWhere defaults | C1: default filter isDeleted=false AND status=Published; admin override via includeAllStatuses/status/statusIn | published default is in place; remaining risk is accidental admin override use in public/generated paths | `app/modules/group/index.ts` |
| Post writes | createPost / updatePost / deletePosts | canonical create/update/delete DB state is wrapped in transactions: localId allocation, post row, attachments, tombstone flag, size, group counters, and shared reply/repost counter repair including status boundary changes | canonical post DB partial-state risk is reduced; source-identity upserts cover relation/group counters plus draft/unpublish/delete reconciliation, with group counter repair drift coverage; post/group manifest rebuilding now defaults to the durable queue, while larger restored-data reruns and static delivery job status still need follow-up | `app/modules/group/index.ts` |
| Post event ledger | post and source-identity import lifecycle | ordinary post create/update/delete and source-identity create/update/delete transitions append PostEvent rows inside the post DB transaction | post and remote import replay/audit risk is reduced for canonical writes; derived manifest/static job events, delivery state, and richer revision payloads remain future work | `app/modules/group/index.ts` |
| Static site rendering | renderGroupPostBatchPages | scans lightweight post refs in cursor batches, uses an exact capped render count for pages, streams SSR through current page/post state, and reuses bounded per-render text/json body and post-object caches | DB hydration and final SSR post data are page-scoped; repeated text/json body reads and repeated reply/repost object conversion are cached within one render without retaining the whole site; remaining large-output pressure is media copy work and genuinely unique post bodies | `app/modules/staticSiteGenerator/index.ts` |
| RSS rendering | groupRss | defaults RSS to a smaller feed window, caps explicit archive reads at the legacy maximum, scans lightweight post refs in cursor batches, then hydrates selected feed batches | default feed generation avoids totals, avoids the old 9999-item window, uses feed batches, reads only the selected feed text body, leaves JSON/non-feed text as metadata, and reuses a bounded feed-local body cache | `app/modules/rss/index.ts` |
| Social import reversal | reverse-post import path | uses lightweight group post refs in (publishedAt,id) cursor batches | reversal avoids content/repost hydration and avoids the previous fixed 10000-row window; localId updates still run row-by-row | `app/modules/socNetImport/index.ts` |
| Remote post import | createPostByRemoteStorageId | remote group manifests compare manifest refs against active local refs, tombstone removed/replaced refs while freeing reused remote local IDs, import missing refs idempotently, preserve manifest local IDs, and skip already-complete groups; remote post manifests preserve source/date fields, claim content manifests into actor-scoped rows, reuse an active same-group manifest row on retry while refreshing the group manifest, and create new local remote posts through the canonical post DB transaction | remote manifest imports now get localId, attachment, counter, group-manifest, post-event, same-manifest retry, group-manifest post iteration, chunked-only post-index coverage, remote edit/delete replay state, and default queued post/group manifest retries consistently; remaining risk is larger restored-data reruns and static delivery job status | `app/modules/remoteGroup/index.ts` |
| Category feeds | getCategoryPosts | joins group/category pivots only for page post ID selection, supports (publishedAt,id) cursor pages and includeTotal=false offset pages, then hydrates contents/group for the bounded page | cursor pages and explicit includeTotal=false offset pages skip counts; legacy offset callers still pay count/offset cost by default | `app/modules/groupCategory/index.ts` |
| Content preview serving | getContentByStorageId(findByPreviews) | legacy/global storage and manifest lookups delegate to deterministic shared helpers, ownerless imports can only reuse existing shared rows, public metadata is projected, and preview mode still ORs across storageId and preview storage ids | shared reads are stable across duplicate user-owned rows, no longer create new ownerless library rows, and the public metadata route no longer exposes private rows by DB id; shared reads still rely on user-library metadata until canonical asset metadata exists | `app/modules/database/index.ts` |
| Content deletion | deleteFileCatalogItem deleteContent | destroys catalog item first; only destroys content/physical storage after DB reference checks, including successful remote pins marked on Content.isPinned | DB row references and locally recorded pin state are covered; generated output, remote pin reconciliation, and async garbage collection still need a fuller lifecycle | `app/modules/fileCatalog/index.ts` |
| File catalog publish | publishFolder / makeFolderChildrenStorageDirsAndCopyFiles | recursively scans child folders and files in id-ordered batches before copying storage files | large folder publishes avoid the public browsing cap and avoid one unbounded child read per folder | `app/modules/fileCatalog/index.ts` |
| Quota checks | checkFileSizeAndSaveByStream | quota sum before upload stream, then content row and upload action commit under a locked UserLimit re-check | visible content/action commits are serialized against the active limit; bytes can still be streamed before final commit rejection, so true pre-stream reservation remains future work | `app/modules/content/index.ts` |
| Async operation ledger | getUserAsyncOperationList / closeAllAsyncOperation / cleanupFinishedAsyncOperations | lists by user/inProcess/name ordered by createdAt, closes in-process rows on startup, and deletes old finished rows in a bounded batch | lookup, startup sweep, queue lifecycle, and finished-row retention cleanup are present; operation history beyond the latest window is intentionally temporary | `app/modules/asyncOperation/index.ts` |
| Auto action executor | claimAutoActionsToExecute | cron atomically claims active due actions in deterministic executeOn/id batches with expiring DB claims before queueing | due/active scan and claim lookup are indexed; multi-node workers skip rows already claimed until the claim expires | `app/modules/autoActions/index.ts` |
| Auto action management list | getUserActions | filters by user plus known fields, applies endpoint sort allowlists/default pages, and returns count-only totals | default user/createdAt/id pages are indexed and capped; alternate sort fields stay bounded by the page cap | `app/modules/autoActions/index.ts` |
| Pin account lookup/list | getUserAccount / getGroupAccount / getUserAccountsList / getGroupAccountsList | looks up pin accounts by owner id plus name and lists owner-scoped accounts with allowlisted sort fields, default pages, and a lower cap | owner/name lookups and bounded owner-scoped lists are backed by the owner/name indexes | `app/modules/pin/index.ts` |
| Foreign account list | getUserAccountsList | API calls list user foreign accounts with allowlisted sort fields, default pages, and a lower cap; manifest export calls the same helper without list params to keep the full account set | user account API pages are bounded and backed by the userId index while manifest generation remains complete | `app/modules/foreignAccounts/index.ts` |
| Social account list | getAccountList | API calls list user social-network accounts with allowlisted sort fields, default pages, a lower cap, and backward-compatible socNet filters | user social-account API pages are bounded and user/socNet lookups keep their composite indexes | `app/modules/socNetAccount/index.ts` |
| Content bot list | content-bot/list | API calls list current-user content bots with allowlisted sort fields, default pages, and a lower cap | user content-bot API pages are bounded and backed by the userId index | `app/modules/tgContentBot/api.ts` |
| List parameter normalization | prepareListParams | normalizes limit/offset, clamps sort direction, enforces endpoint sort allowlists, supports includeTotal opt-out, and caps public post feeds, file-catalog browsing, category management lists, static-site lists, operation queue lists, invite lists, API-key lists, admin directory lists, user group membership/chat lists, user friend lists, auto-action management lists, pin account lists, foreign account API lists, social account API lists and content-bot API lists below the global export ceiling | public post feeds, file-catalog browsing, category management lists, static-site lists, operation queue lists, invite lists, API-key lists, admin directory lists, user group membership/chat lists, user friend lists, auto-action management lists, pin account lists, foreign account API lists, social account API lists and content-bot API lists use endpoint allowlists and lower caps; post feeds can opt out of totals; remaining endpoints still need endpoint-specific policies | `app/helpers.ts` |
| Static rebind scan | getGroupWhereStaticOutdated | filters non-deleted groups whose staticStorageUpdatedAt is older than the threshold, ordered oldest-first in a capped batch | scan predicate is indexed and bounded for manual/ops use; cron remains disabled and should become event-driven before high churn | `app/modules/group/index.ts` |
| Static ID resolution | getStaticIdItemByDynamicId | current-binding lookup by dynamicId with a guarded history fallback/lazy fill for model-sync upgrades | hot static-ID resolution uses compact current rows; bounded per-static history cleanup preserves current bindings while capping new churn | `app/modules/staticId/index.ts` |

