# Database Scalability Inventory

## Source Of Truth

Original request for this slice: add a review of database scalability for storing hundreds of thousands of posts and content records in groups.

This file is generated by `npm run database:scalability:update`. Do not hand-edit the tables; update `check/databaseScalabilityInventory.ts` or the source code and regenerate.

## Summary

- Model/query areas reviewed: 17
- Query hotspots reviewed: 21
- Primary risk area: group post timelines and generated manifests at 100k+ posts per group.
- Current review doc: [database-scalability-review.md](database-scalability-review.md).

## Model Index Inventory

| Area | Model | Current Index Signals | Review Notes | Source |
| --- | --- | --- | --- | --- |
| Group posts | Post | name; name,isRemote unique local; replyToId; repostOfId; source,sourceDate; source,sourceChannelId; source,sourceChannelId,sourcePostId; groupId,source,sourceChannelId,sourcePostId unique source identity | has group index; has group/local unique identity; has group/source unique import identity; has manifest lookup index | `app/modules/group/models/post.ts` |
| Post contents join | PostsContents | contentId,postId through primary key; postId,position unique order index; contentId reverse index | join lookup indexes present; attachment positions are unique per post; API timeline hydration is page-scoped; remaining work is body projection | `app/modules/group/models/post.ts` |
| Groups | Group | name,isRemote unique local non-collateral; manifestStorageId; manifestStaticStorageId; creatorId,type,isDeleted,createdAt,id creator list index; isDeleted,staticStorageUpdatedAt static rebind index | has creator listing index; membership/admin through indexes present | `app/modules/group/models/group.ts` |
| Contents | Content | userId,storageId unique ownership constraint plus storageId,userId lookup; manifestStaticStorageId unique; userId,manifestStorageId actor-scoped lookup | has user listing index; has manifest lookup index; has preview lookup indexes; same storageId across different users remains valid; same-user duplicates are guarded by cleanup-backed uniqueness; shared storage/manifest reads use deterministic id ordering while actor/canonical semantics remain caller-specific | `app/modules/database/models/content.ts` |
| Object cache | Object | storageId,resolveProp unique cache key | active data-structure cache keys resolved and unresolved storage paths separately | `app/modules/database/models/object.ts` |
| File catalog | FileCatalogItem | contentId reverse index; user,parent,isDeleted,type,createdAt,id folder listing index; cleanup-backed active path uniqueness for child and root rows | content reference checks, large folder listings, and exact active path writes are covered | `app/modules/fileCatalog/models.ts` |
| Static ID history | StaticIdHistory / StaticIdBinding | staticId,dynamicId unique; staticId,boundAt; dynamicId lookup index; staticId unique current binding; dynamicId,boundAt current lookup | hot static and dynamic resolution use compact current-binding rows; StaticIdHistory remains audit history and still needs retention/compaction policy | `app/modules/staticId/models.ts` |
| Content accounting | UserContentAction / UserLimit | content action user index; user limit user,name unique | quota checks sum UserContentAction by userId/name/createdAt before uploads; ledger size is BIGINT; UserLimit is unique per user/name after duplicate cleanup and duplicate-key retry | `app/modules/database/models/userContentAction.ts` |
| Async operations | UserAsyncOperation / UserOperationQueue | userId,inProcess,name,createdAt operation-list index; inProcess,updatedAt restart-sweep index; queue module,isWaiting picker index; queue asyncOperationId lifecycle index; queue isWaiting,asyncOperationId,updatedAt,id retention index | operation-list, restart-sweep, queue lifecycle, and queue retention indexes are present; finished-operation retention cleanup is bounded on startup | `app/modules/asyncOperation/models.ts` |
| Auto actions | AutoAction | isActive index; isActive,executeOn executor index; isActive,executeOn,executeClaimExpiresAt,id claim index; userId,createdAt,id management-list index | due active executor scan, execution claims, and user management-list default paging are indexed; cron claims due actions atomically before queueing | `app/modules/autoActions/models.ts` |
| Pin accounts | PinAccount | cleanup-backed userId,name unique lookup; cleanup-backed groupId,name unique lookup | runtime owner/name lookups are deterministic after duplicate-name cleanup | `app/modules/pin/models.ts` |
| Group permissions | GroupPermission | explicit permission index | permission lookup indexes present; uniqueness/share semantics still separate | `app/modules/group/models/groupPermission.ts` |
| Unread state | GroupRead | userId,groupId unique | supports one read cursor per user/group; unread count can use readAt plus readPostId tie-breaker | `app/modules/group/models/groupRead.ts` |
| Social imports | SocNetImport* | channel/message source indexes present | message duplicate checks and post source identity are constraint-backed; reversal paths use lightweight post refs when present | `app/modules/socNetImport/models.ts` |
| Static sites | StaticSite | entity/user lookup indexes present | static generation scalability is dominated by group post loading, not staticSite lookup | `app/modules/staticSiteGenerator/models.ts` |
| Categories and sections | GroupCategory / GroupSection pivots | category name; section name,categoryId unique; section parentSectionId; categoryGroups categoryId,groupId; categoryGroups groupId,categoryId; groupSections sectionId,groupId; groupSections groupId | category admin/member/group through indexes are present; section pivot indexes are present; feed cursor/count policy still needs review | `app/modules/groupCategory/models/groupCategory.ts` |
| Tags and mentions | Tag / TaggedPosts / Mention / AutoTag | tag name index; taggedPosts post/tag indexes; mention FK indexes; autoTag group index | tag/mention/auto-tag lookup indexes are present; feed cursor/count policy still needs review before large tag surfaces | `app/modules/group/models/tag.ts` |

## Query Hotspots

| Area | Hotspot | Observed Pattern | Scalability Risk | Source |
| --- | --- | --- | --- | --- |
| Timeline API | getGroupPosts | keyset/cursor and legacy offset paths select page post IDs first, hydrate contents/reposts for the bounded page, and allow offset callers to skip totals | page selection avoids attachment joins; cursor pages and includeTotal=false skip counts, while default legacy offset callers can still request totals | `app/modules/group/index.ts` |
| Global post listings | getAllPosts | selects cursor-aware page post refs first, then hydrates contents for the bounded page; exports can iterate lightweight refs in cursor batches | content joins no longer drive page selection; high-volume all-post scans can avoid offsets through cursor ref batches | `app/modules/group/index.ts` |
| Unread counters | getGroupUnreadPostsData | count posts newer than the stored (readAt, readPostId) cursor when present, with readAt-only fallback | count is backed by the timeline index and can disambiguate same-timestamp posts; old readAt-only rows still use the legacy timestamp predicate | `app/modules/group/index.ts` |
| Group manifest generation | generateGroupManifest | loads the previous posts trie, scans changed/deleted/unpublished lightweight post refs in (updatedAt,id) cursor batches, then unsets removed local IDs | content/repost hydration and large changed-ref windows are avoided; rebuild still loads/copies the previous posts trie and rewrites a monolithic manifest | `app/modules/entityJsonManifest/index.ts` |
| Post visibility | getPostsWhere defaults | C1: default filter isDeleted=false AND status=Published; admin override via includeAllStatuses/status/statusIn | published default is in place; remaining risk is accidental admin override use in public/generated paths | `app/modules/group/index.ts` |
| Post writes | createPost / updatePost / deletePosts | canonical create/update/delete DB state is wrapped in transactions: localId allocation, post row, attachments, tombstone flag, size, group counters, and reply/repost counts including status boundary changes | canonical post DB partial-state risk is reduced; import/upsert transitions and manifest/static derived work still need transaction/job boundaries | `app/modules/group/index.ts` |
| Static site rendering | prepareGroupPostsForRender | scans lightweight post refs in cursor batches, then hydrates and renders each bounded batch | large exports still materialize final render data and copy content, but DB hydration is page-scoped | `app/modules/staticSiteGenerator/index.ts` |
| RSS rendering | groupRss | scans lightweight post refs in cursor batches, then hydrates selected feed batches | feed generation avoids total counts and the previous 9999-row hydration query; body extraction still reads selected content files | `app/modules/rss/index.ts` |
| Social import reversal | reverse-post import path | uses lightweight group post refs in (publishedAt,id) cursor batches | reversal avoids content/repost hydration and avoids the previous fixed 10000-row window; localId updates still run row-by-row | `app/modules/socNetImport/index.ts` |
| Category feeds | getCategoryPosts | joins group/category pivots only for page post ID selection, supports (publishedAt,id) cursor pages and includeTotal=false offset pages, then hydrates contents/group for the bounded page | cursor pages and explicit includeTotal=false offset pages skip counts; legacy offset callers still pay count/offset cost by default | `app/modules/groupCategory/index.ts` |
| Content preview serving | getContentByStorageId(findByPreviews) | legacy/global storage and manifest lookups delegate to deterministic shared helpers; preview mode still ORs across storageId and preview storage ids | shared reads are stable across duplicate user-owned rows, but they still read user-library metadata until canonical asset metadata exists | `app/modules/database/index.ts` |
| Content deletion | deleteFileCatalogItem deleteContent | destroys catalog item first; only destroys content/physical storage after DB reference checks | DB row references are covered; generated output, durable pin state, and async garbage collection still need a fuller lifecycle | `app/modules/fileCatalog/index.ts` |
| File catalog publish | publishFolder / makeFolderChildrenStorageDirsAndCopyFiles | recursively scans child folders and files in id-ordered batches before copying storage files | large folder publishes avoid the public browsing cap and avoid one unbounded child read per folder | `app/modules/fileCatalog/index.ts` |
| Quota checks | checkFileSizeAndSaveByStream | quota sum before upload stream, action recorded after content creation | accounting SUM is indexed, but concurrent uploads can still pass before reservation/action recording | `app/modules/content/index.ts` |
| Async operation ledger | getUserAsyncOperationList / closeAllAsyncOperation / cleanupFinishedAsyncOperations | lists by user/inProcess/name ordered by createdAt, closes in-process rows on startup, and deletes old finished rows in a bounded batch | lookup, startup sweep, queue lifecycle, and finished-row retention cleanup are present; operation history beyond the latest window is intentionally temporary | `app/modules/asyncOperation/index.ts` |
| Auto action executor | claimAutoActionsToExecute | cron atomically claims active due actions in deterministic executeOn/id batches with expiring DB claims before queueing | due/active scan and claim lookup are indexed; multi-node workers skip rows already claimed until the claim expires | `app/modules/autoActions/index.ts` |
| Auto action management list | getUserActions | filters by user plus known fields, applies endpoint sort allowlists/default pages, and returns count-only totals | default user/createdAt/id pages are indexed and capped; alternate sort fields stay bounded by the page cap | `app/modules/autoActions/index.ts` |
| Pin account lookup | getUserAccount / getGroupAccount | looks up pin accounts by owner id plus name | owner/name lookups are indexed and uniqueness-backed | `app/modules/pin/index.ts` |
| List parameter normalization | prepareListParams | normalizes limit/offset, clamps sort direction, enforces endpoint sort allowlists, supports includeTotal opt-out, and caps public post feeds, file-catalog browsing, category management lists, static-site lists, operation queue lists, invite lists, API-key lists, admin directory lists, user group membership/chat lists, user friend lists and auto-action management lists below the global export ceiling | public post feeds, file-catalog browsing, category management lists, static-site lists, operation queue lists, invite lists, API-key lists, admin directory lists, user group membership/chat lists, user friend lists and auto-action management lists use endpoint allowlists and lower caps; post feeds can opt out of totals; remaining endpoints still need endpoint-specific policies | `app/helpers.ts` |
| Static rebind scan | getGroupWhereStaticOutdated | filters non-deleted groups whose staticStorageUpdatedAt is older than the threshold | scan predicate is indexed; cron remains disabled and should probably become event-driven before high churn | `app/modules/group/index.ts` |
| Static ID resolution | getStaticIdItemByDynamicId | current-binding lookup by dynamicId with a guarded history fallback for pre-backfill rows | hot static-ID resolution uses compact current rows; audit-history retention still needs policy | `app/modules/staticId/index.ts` |

